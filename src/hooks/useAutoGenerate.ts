
// This hook has been removed to fix infinite re-render issues
// The functionality has been moved to direct button calls in TranslationResults
export const useAutoGenerate = () => {
  // No-op hook to maintain compatibility during transition
};
